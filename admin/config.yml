backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

site_url: https://dymiec.com.mx
display_url: https://dymiec.com.mx

collections:
  - name: "servicios"
    label: "Servicios"
    folder: "servicios"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Título", name: "title", widget: "string" }
      - { label: "Contenido", name: "body", widget: "markdown" }